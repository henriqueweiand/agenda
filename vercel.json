{
    "version": 2,
    "scope": "meagenda",
    "github": {
        "enabled": false
    },
    "builds": [
        {
            "src": "/dist/apps/api/main.js",
            "use": "@now/node"
        }
    ],
    "routes": [
        { "src": "/api/(.*)", "dest": "/dist/apps/api/main.js" },
        { "handle": "filesystem" }
    ]
}
